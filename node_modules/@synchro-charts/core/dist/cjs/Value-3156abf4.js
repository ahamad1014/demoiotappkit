'use strict';

const index = require('./index-0b3ed36d.js');
const terms = require('./terms-4f03f33b.js');
const number = require('./number-46ecc337.js');

/**
 * Display value of a data point, supports all data types
 */
const Value = ({ isEnabled = true, value, unit }) => {
    if (!isEnabled || value == null) {
        return index.h("span", { "data-testid": "no-value-present" }, terms.NO_VALUE_PRESENT);
    }
    if (typeof value === 'number') {
        /** Display Number */
        return [number.round(value), unit && index.h("span", { class: "unit" },
                " ",
                unit)];
    }
    /** Display String or Booleans */
    return [String(value), unit && index.h("span", { class: "unit" },
            " ",
            unit)];
};

exports.Value = Value;
