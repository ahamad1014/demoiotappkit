'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const index = require('./index-0b3ed36d.js');

const ScBox = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.size = { width: 0, height: 0 };
    }
    render() {
        const dataProps = this.someObject
            ? Object.keys(this.someObject).reduce((obj, key) => (Object.assign(Object.assign({}, obj), { [`data-${key}`]: this.someObject[key] })), {})
            : {};
        return (index.h("div", Object.assign({}, dataProps, { class: "box-container", style: {
                boxSizing: 'border-box',
                border: '1px solid black',
                backgroundColor: 'lightgrey',
                width: `${this.size.width}px`,
                height: `${this.size.height}px`,
            } }), index.h("ul", null, index.h("li", null, this.size.width, " width"), index.h("li", null, this.size.height, " height"))));
    }
};

exports.sc_box = ScBox;
