'use strict';

const dataConstants = require('./dataConstants-02642889.js');

const isDefined = (value) => value != null;
const isValid = (predicate) => (t) => predicate(t);
// As of now, we only check if the current component supports string or not.
const isSupportedDataType = (supportsString) => ({ dataType }) => (supportsString && dataType === dataConstants.DataType.STRING) || dataType !== dataConstants.DataType.STRING;
const isNumberDataStream = (stream) => stream.dataType === dataConstants.DataType.NUMBER;
const isNumber = (val) => typeof val === 'number';
const isMinimalStaticViewport = (viewport) => 'start' in viewport && 'end' in viewport;

exports.isDefined = isDefined;
exports.isMinimalStaticViewport = isMinimalStaticViewport;
exports.isNumber = isNumber;
exports.isNumberDataStream = isNumberDataStream;
exports.isSupportedDataType = isSupportedDataType;
exports.isValid = isValid;
