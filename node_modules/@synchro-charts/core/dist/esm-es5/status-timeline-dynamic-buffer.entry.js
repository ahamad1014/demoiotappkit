var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import { r as registerInstance, h } from './index-0c943849.js';
import { D as DataType } from './dataConstants-856cb1e9.js';
import './index-b3f1c3a1.js';
import { a as MONTH_IN_MS } from './time-b732648c.js';
// viewport boundaries
var Y_MIN = 0;
var Y_MAX = 5000;
var X_MIN = new Date(1998, 0, 0);
var X_MAX = new Date(2000, 0, 1);
var WIDTH = X_MAX.getTime() - X_MIN.getTime();
var StatusTimelineDynamicBuffer = /** @class */ (function () {
    function StatusTimelineDynamicBuffer(hostRef) {
        var _this = this;
        registerInstance(this, hostRef);
        this.data = [];
        this.addDataPoint = function () {
            var dataPoint = {
                // To generate new status with half of the distance.
                x: X_MIN.getTime() + WIDTH / (2 + _this.data.length),
                y: 2.5,
            };
            _this.data = __spreadArrays([dataPoint], _this.data);
        };
    }
    StatusTimelineDynamicBuffer.prototype.render = function () {
        var _a;
        return (h("div", { class: "synchro-chart-tests" }, h("button", { id: "add-data-point", onClick: this.addDataPoint }, "Add Data Point"), h("div", { id: "chart-container", style: { height: '500px', width: '500px', marginTop: '20px' } }, h("sc-status-timeline", { widgetId: "widget-id", alarms: { expires: MONTH_IN_MS }, dataStreams: [
                {
                    id: 'test',
                    color: 'red',
                    name: 'test stream',
                    aggregates: (_a = {}, _a[MONTH_IN_MS] = this.data, _a),
                    data: [],
                    resolution: MONTH_IN_MS,
                    dataType: DataType.NUMBER,
                },
            ], viewport: {
                yMin: Y_MIN,
                yMax: Y_MAX,
                start: X_MIN,
                end: X_MAX,
            }, bufferFactor: 1, minBufferSize: 1 }), h("sc-webgl-context", null))));
    };
    return StatusTimelineDynamicBuffer;
}());
export { StatusTimelineDynamicBuffer as status_timeline_dynamic_buffer };
