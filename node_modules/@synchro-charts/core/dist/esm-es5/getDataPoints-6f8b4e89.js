/**
 * Get the points for a given resolution from a data stream
 */
var getDataPoints = function (stream, resolution) {
    if (resolution === 0) {
        return stream.data;
    }
    if (stream.aggregates == null) {
        return [];
    }
    return stream.aggregates[resolution] || [];
};
export { getDataPoints as g };
