export const getX = ({ annotation, width, viewport, }) => {
    const { start, end } = viewport;
    return Math.floor((width / (end.getTime() - start.getTime())) * (annotation.value.getTime() - start.getTime()));
};
