import { Component, h } from '@stencil/core';
import { routes } from '../app/routes';
export class testingGrounds {
    render() {
        return (h("div", null,
            h("h3", null,
                "hello, welcome to the",
                ' ',
                h("a", { href: "https://synchrocharts.com/", rel: "noopener noreferrer", target: "_blank" }, "synchro-charts"),
                ' ',
                "testing grounds!"),
            h("h4", null, "check out our demo components:"),
            routes.map(route => (h("div", null,
                ' ',
                h("a", { href: route.url }, route.component),
                ' ')))));
    }
    static get is() { return "testing-ground"; }
}
