import { AlarmsConfig, DataStream, MessageOverrides, MinimalSizeConfig, MinimalViewPortConfig } from '../../../utils/dataTypes';
import { Annotations, Axis, ChartConfig, LayoutConfig, LegendConfig, MovementConfig, ScaleConfig } from '../common/types';
import { Trend } from '../common/trends/types';
export declare class ScScatterChart implements ChartConfig {
    /** Chart API */
    viewport: MinimalViewPortConfig;
    movement?: MovementConfig;
    scale?: ScaleConfig;
    layout?: LayoutConfig;
    legend?: LegendConfig;
    size?: MinimalSizeConfig;
    widgetId: string;
    dataStreams: DataStream[];
    alarms?: AlarmsConfig;
    gestures: boolean;
    annotations: Annotations;
    trends: Trend[];
    axis?: Axis.Options;
    messageOverrides?: MessageOverrides;
    /** Status */
    isEditing: boolean;
    /** Memory Management */
    bufferFactor: number;
    minBufferSize: number;
    componentWillRender(): void;
    render(): any;
}
