import { XAnnotation } from '../../types';
import { ViewPort } from '../../../../../utils/dataTypes';
export declare const getX: ({ annotation, width, viewport, }: {
    annotation: XAnnotation;
    width: number;
    viewport: ViewPort;
}) => number;
