import { XAnnotation } from '../../types';
import { ViewPort } from '../../../../../utils/dataTypes';
export declare const TEXT_SELECTOR = "text.x-text";
export declare const ANNOTATION_GROUP_SELECTOR = "g.x-annotation";
export declare const LINE_SELECTOR = "line.x-line";
export declare const renderXAnnotations: ({ container, xAnnotations, viewport, resolution, size: { width, height }, }: {
    container: SVGElement;
    xAnnotations: XAnnotation[];
    viewport: ViewPort;
    resolution: number;
    size: {
        width: number;
        height: number;
    };
}) => void;
export declare const removeXAnnotations: ({ container }: {
    container: SVGElement;
}) => void;
