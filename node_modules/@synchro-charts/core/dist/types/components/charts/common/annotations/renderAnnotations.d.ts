import { Annotations } from '../types';
import { DataStream, ViewPort } from '../../../../utils/dataTypes';
import { DraggableAnnotationsOptions } from './draggableAnnotations';
export declare type RenderAnnotationsOptions = {
    container: SVGElement;
    resolution: number;
    annotations: Annotations;
    viewport: ViewPort;
    size: {
        width: number;
        height: number;
    };
    onUpdate: ({ start, end }: {
        start: Date;
        end: Date;
    }, hasDataChanged: boolean, hasSizeChanged: boolean, hasAnnotationChanged: boolean) => void;
    activeViewPort: () => ViewPort;
    emitUpdatedWidgetConfiguration: (dataStreams?: DataStream[]) => void;
    draggable: (draggableOptions: DraggableAnnotationsOptions) => void;
    startStopDragging: (dragState: boolean) => void;
    inDragState: () => boolean;
};
export declare const renderAnnotations: ({ container, resolution, annotations, viewport, size, onUpdate, activeViewPort, emitUpdatedWidgetConfiguration, draggable, startStopDragging, inDragState, }: RenderAnnotationsOptions) => void;
