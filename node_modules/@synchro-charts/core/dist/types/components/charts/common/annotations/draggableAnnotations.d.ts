import { Selection } from 'd3-selection';
import { DataStream, ViewPort } from '../../../../utils/dataTypes';
export declare type DraggableAnnotationsOptions = {
    container: SVGElement;
    size: {
        width: number;
        height: number;
    };
    onUpdate: ({ start, end }: {
        start: Date;
        end: Date;
    }, hasDataChanged: boolean, hasSizeChanged: boolean, hasAnnotationChanged: boolean) => void;
    activeViewPort: () => ViewPort;
    emitUpdatedWidgetConfiguration: (dataStreams?: DataStream[]) => void;
    startStopDragging: (dragState: boolean) => void;
    resolution: number;
    dragHandle: Selection<any, any, any, any>;
};
export declare const FOCUS_TRANSITION_TIME = 100;
/**
 * Draggable Thresholds Feature
 */
export declare const attachDraggable: () => ({ container, size, onUpdate, activeViewPort, emitUpdatedWidgetConfiguration, startStopDragging, dragHandle, resolution, }: DraggableAnnotationsOptions) => void;
