import { RenderTrendLinesOptions } from './types';
export declare const renderTrendLines: ({ container, viewport, size: { width, height }, dataStreams, trendResults, }: RenderTrendLinesOptions) => void;
