declare type ContextPlacement = 'viewport' | 'element';
/**
 * Testing route for the webGL rendering without being fully coupled to the chart.
 *
 * Tests that a single point renders as a circle correctly
 */
export declare class ScWebglContextNested {
    elementRef?: HTMLDivElement;
    selectList?: HTMLSelectElement;
    contextPlacement: ContextPlacement;
    render(): any;
}
export {};
