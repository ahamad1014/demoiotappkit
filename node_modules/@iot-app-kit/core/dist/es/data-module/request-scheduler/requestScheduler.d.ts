export default class RequestScheduler {
    private intervalMap;
    constructor();
    create({ id, refreshRate, refreshExpiration, cb, synchronize, }: {
        id: string;
        refreshRate?: number;
        refreshExpiration?: number;
        cb: () => void;
        synchronize?: boolean;
    }): void;
    remove(id: string): void;
    isScheduled: (id: string) => boolean;
    private handleVisibilityChange;
    private synchronizeIntervals;
    private resetInterval;
}
//# sourceMappingURL=requestScheduler.d.ts.map