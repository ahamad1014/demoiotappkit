import type { DataStreamsStore } from './types';
import type { DataStream, RequestInformation } from '../types';
/**
 * To Data Streams
 *
 * Returns the data streams, with the various resolutions of data collapsed into a single corresponding data stream.
 */
export declare const toDataStreams: ({ requestInformations, dataStreamsStores, }: {
    requestInformations: RequestInformation[];
    dataStreamsStores: DataStreamsStore;
}) => DataStream[];
//# sourceMappingURL=toDataStreams.d.ts.map