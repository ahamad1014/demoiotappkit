import { Interval } from '../../common/intervalStructure';
import type { DateInterval, Viewport } from './requestTypes';
/**
 * Collect the errors across the relevant data stream infos
 */
export declare const getDateInterval: (viewport: Viewport) => DateInterval;
export declare const hasIntervalForRange: (intervals: Interval[], range: {
    start: Date;
    end: Date;
}) => boolean;
//# sourceMappingURL=dateUtils.d.ts.map