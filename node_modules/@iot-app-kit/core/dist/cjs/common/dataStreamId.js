"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toSiteWiseAssetProperty = exports.toDataStreamId = void 0;
// Something that is not likely to occur in any UUID implementation
const ID_SEPARATOR = '---';
const toDataStreamId = ({ assetId, propertyId, }) => `${assetId}${ID_SEPARATOR}${propertyId}`;
exports.toDataStreamId = toDataStreamId;
const toSiteWiseAssetProperty = (dataStreamId) => {
    if (!dataStreamId.includes(ID_SEPARATOR)) {
        throw new Error(`Invalid id ${dataStreamId}, expected to find the separator ${ID_SEPARATOR} but it was not found`);
    }
    const [assetId, propertyId] = dataStreamId.split(ID_SEPARATOR);
    return {
        assetId,
        propertyId,
    };
};
exports.toSiteWiseAssetProperty = toSiteWiseAssetProperty;
