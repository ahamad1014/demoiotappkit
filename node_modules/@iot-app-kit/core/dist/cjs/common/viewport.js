"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.viewportEndDate = exports.viewportStartDate = void 0;
const predicates_1 = require("./predicates");
const time_1 = require("./time");
const viewportStartDate = (viewport, currentDate) => (0, predicates_1.isHistoricalViewport)(viewport)
    ? new Date(viewport.start)
    : new Date(((currentDate === null || currentDate === void 0 ? void 0 : currentDate.getTime()) || Date.now()) -
        (0, time_1.parseDuration)(viewport.duration));
exports.viewportStartDate = viewportStartDate;
const viewportEndDate = (viewport, currentDate) => {
    return (0, predicates_1.isHistoricalViewport)(viewport)
        ? new Date(viewport.end)
        : currentDate || new Date(Date.now());
};
exports.viewportEndDate = viewportEndDate;
