import { Component, h } from '@stencil/core';
import { initialize } from '@iot-app-kit/source-iotsitewise';
import { getEnvCredentials } from '../testing-ground/getEnvCredentials';
export class IotResourceExplorerDemo {
  componentWillLoad() {
    const { query } = initialize({
      awsCredentials: getEnvCredentials(),
      awsRegion: 'us-east-1',
    });
    this.query = query;
  }
  render() {
    return h("iot-resource-explorer", { query: this.query.assetTree.fromRoot() });
  }
  static get is() { return "iot-resource-explorer-demo"; }
  static get originalStyleUrls() { return {
    "$": ["../../styles/awsui.css"]
  }; }
  static get styleUrls() { return {
    "$": ["../../styles/awsui.css"]
  }; }
}
